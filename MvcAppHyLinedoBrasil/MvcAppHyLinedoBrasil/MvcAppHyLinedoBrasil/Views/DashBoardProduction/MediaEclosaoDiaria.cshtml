@{
    ViewBag.Title = "% Média de Eclosão p/ Dia";
}

<script type="text/javascript" src="https://www.google.com/jsapi"></script>
    <script type="text/javascript">
        google.load("visualization", "1", { packages: ["corechart"] });
        google.setOnLoadCallback(drawChart);

        function drawChart() {
            var data = new google.visualization.DataTable(@(Html.Raw(ViewBag.MediaEclosaoDiaria)));

            var options = {
                title: '% Média de Eclosão p/ Dia',
                hAxis: {gridlines: {count: @ViewBag.QtdeRegistrosEixoXMediaEclosaoDiaria }, format: 'dd'},
                height: 400,
                chartArea: {width:'75%',height:'80%'},
                legend: {position: 'bottom'},
                trendlines: { 0: {} }
            };

            var chart = new google.visualization.LineChart(document.getElementById('chartMediaEclosaoDiaria'));
            chart.draw(data, options);
        }
    </script>
<div>
    @if (ViewBag.QtdeRegistrosEixoXMediaEclosaoDiaria != 0)
    {
    <div id="chartMediaEclosaoDiaria" style="width: 100%; height: 100%;"></div>
    }
    else
    {
    <h3>
        % Média de Eclosão p/ Dia
        <br />
        Não existem dados para os parâmetros selecionados!
    </h3>
    }
</div>