@{
    ViewBag.Title = "Capacidade do Incubatório p/ Dia";
}

<script type="text/javascript" src="https://www.google.com/jsapi"></script>
    <script type="text/javascript">
        google.load("visualization", "1", { packages: ["corechart"] });
        google.setOnLoadCallback(drawChart);

        function drawChart() {
            var data = new google.visualization.DataTable(@(Html.Raw(ViewBag.CapacidadeDiaria)));

            var options = {
                title: 'Capacidade do Incubatório p/ Dia',
                hAxis: {gridlines: {count: @ViewBag.QtdeRegistrosEixoXCapacidadeDiaria }, format: 'dd'},
                height: 400,
                chartArea: {width:'75%',height:'80%'},
                legend: {position: 'bottom'},
                trendlines: { 1: {} }
            };

            var chart = new google.visualization.LineChart(document.getElementById('chartCapacidadeDiaria'));
            chart.draw(data, options);
        }
    </script>
<div>
    @if (@ViewBag.QtdeRegistrosEixoXCapacidadeDiaria != 0)
    {
    <div id="chartCapacidadeDiaria" style="width: 100%; height: 100%;"></div>
    }
    else
    {
    <h3>
        Capacidade do Incubatório p/ Dia
        <br />
        Não existem dados para os parâmetros selecionados!
    </h3>
    }
</div>